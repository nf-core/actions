* text=auto eol=lf

# Committed, generated bundles. Collapsed by default in pull request diffs,
# but still full text diffs (not "-diff") so reviewers can expand and read them.
actions/*/dist/** linguist-generated=true
